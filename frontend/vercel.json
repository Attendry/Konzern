{
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "dist",
  "installCommand": "git config --global user.name 'Attendry' || true && git config --global user.email 'attendry@example.com' || true && if [ -f package-lock.json ]; then npm ci; else npm install; fi",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
