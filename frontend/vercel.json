{
  "buildCommand": "(git config user.name 'Attendry' || true) && (git config user.email 'attendry@example.com' || true) && npm ci && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm ci",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
